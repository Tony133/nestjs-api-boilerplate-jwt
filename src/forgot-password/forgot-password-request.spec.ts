import { ForgotPasswordRequest } from './forgot-password-request';

describe('ForgotPasswordRequest', () => {
  it('should be defined', () => {
    expect(new ForgotPasswordRequest()).toBeDefined();
  });
});
