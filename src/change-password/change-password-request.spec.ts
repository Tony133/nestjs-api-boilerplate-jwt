import { ChangePasswordRequest } from './change-password-request';

describe('ChangePasswordRequest', () => {
  it('should be defined', () => {
    expect(new ChangePasswordRequest()).toBeDefined();
  });
});
